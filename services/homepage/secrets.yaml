secrets:
  homepage:
    data:
      # Sonarr API Key - Replace with actual value before encrypting with SOPS
      sonarrApiKey: "REPLACE_WITH_SONARR_API_KEY"
      # Radarr API Key - Replace with actual value before encrypting with SOPS
      radarrApiKey: "REPLACE_WITH_RADARR_API_KEY"
      # Lidarr API Key - Replace with actual value before encrypting with SOPS
      lidarrApiKey: "REPLACE_WITH_LIDARR_API_KEY"
      # Bazarr API Key - Replace with actual value before encrypting with SOPS
      bazarrApiKey: "REPLACE_WITH_BAZARR_API_KEY"
      # Prowlarr API Key - Replace with actual value before encrypting with SOPS
      prowlarrApiKey: "REPLACE_WITH_PROWLARR_API_KEY"
      # Jellyseerr API Key - Replace with actual value before encrypting with SOPS
      jellyseerrApiKey: "REPLACE_WITH_JELLYSEERR_API_KEY"
      # qBittorrent credentials - Replace with actual values before encrypting with SOPS
      qbittorrentUsername: "REPLACE_WITH_QBITTORRENT_USERNAME"
      qbittorrentPassword: "REPLACE_WITH_QBITTORRENT_PASSWORD"
      # Grafana credentials - Replace with actual values before encrypting with SOPS
      grafanaUsername: "REPLACE_WITH_GRAFANA_USERNAME"
      grafanaPassword: "REPLACE_WITH_GRAFANA_PASSWORD"
