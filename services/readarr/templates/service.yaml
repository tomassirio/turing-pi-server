apiVersion: v1
kind: Service
metadata:
  name: {{ include "readarr.fullname" . }}
  labels:
    app: {{ include "readarr.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      name: http
      targetPort: {{ .Values.webuiPort }}
  selector:
    app: {{ include "readarr.name" . }}

