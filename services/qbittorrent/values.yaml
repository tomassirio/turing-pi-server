replicaCount: 1

image:
  repository: linuxserver/qbittorrent
  tag: latest
  pullPolicy: IfNotPresent

# This is for the secretes for pulling an image from a private repository more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/pull-image-private-registry/
imagePullSecrets: []
# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""

# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations:
  prometheus.io/scrape: "true"
  prometheus.io/port: "9091"
  prometheus.io/path: "/metrics"

additionalContainers:
  - name: exporter
    image: ghcr.io/esanchezm/prometheus-qbittorrent-exporter:latest
    imagePullPolicy: IfNotPresent
    env:
      - name: QBITTORRENT_HOST
        value: "localhost"
      - name: QBITTORRENT_PORT
        value: "8080"
      - name: QBITTORRENT_USER
        value: "admin"
      - name: QBITTORRENT_PASS
        value: "adminadmin"
      - name: EXPORTER_PORT
        value: "9091"
    ports:
      - name: metrics
        containerPort: 9091
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  type: ClusterIP
  port: 80

webuiPort: 8080
torrentPort: 6881

persistence:
  root:
    enabled: true
    existingClaim: nas-pvc
  config:
    enabled: true
    existingClaim: nas-config-pvc

ingress:
  enabled: true
  className: nginx
  host: qbittorrent.localhome.com
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "0"

env:
  PUID: "1000"
  PGID: "1000"

resources:
  requests:
    cpu: 500m
    memory: 1Gi
  limits:
    cpu: 2000m
    memory: 3Gi

# This section is for setting up autoscaling more information can be found here: https://kubernetes.io/docs/concepts/workloads/autoscaling/
autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 75
  # targetMemoryUtilizationPercentage: 80

# This is to setup the liveness and readiness probes more information can be found here: https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/
livenessProbe:
  httpGet:
    path: /
    port: http
readinessProbe:
  httpGet:
    path: /
    port: http


# This is for setting up the VPN more information can be found here: https://github.com/qdm12/gluetun-wiki/tree/main
vpn:
  enabled: true
  secretName: protonvpn-secret
  provider: protonvpn
  type: openvpn
  countries: "Switzerland,Iceland,Canada,Brazil,Malaysia,Japan,South Africa,New Zealand,Sweden,Panama"
  dot: "off"
  dnsAddress: "1.1.1.1"
  healthTargetAddress: "cloudflare.com"
  image:
    repository: qmcgaw/gluetun
    tag: latest
    pullPolicy: IfNotPresent
  securityContext:
    capabilities:
      add:
        - NET_ADMIN
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 500m
      memory: 256Mi