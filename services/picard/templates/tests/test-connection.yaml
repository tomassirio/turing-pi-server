apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "picard.fullname" . }}-test-connection"
  labels:
    {{- include "picard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "picard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
